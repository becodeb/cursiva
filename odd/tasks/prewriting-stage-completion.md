# Finishing the pre-writing stage

Feature document (ODD). Engram mirror: topic `odd/prewriting-stage-completion/tasks`, project `cursiva`.
Source: the user's play-test on 2026-09-25, after `main` was published at `a1b1eed`.

## Objective

Make the pre-writing stage playable end to end by a first-grade child, then make it feel like one
game: full-screen art, UI in the game's style, a detective loop that gives every trace a purpose.
Work ships in batches; each batch is merged to `main` and pushed so the user can test the deploy
before the next one starts.

## Problem (what the user hit)

- Snakes: impossible to pass; the corridor does not line up with the snake art; no goal or story.
  The user could not get past it, so bee, dolphins, fish, turtles, monkeys and the finale are untested.
- Flashlight (night): the first tap lights up the whole level; on the others a halo already shows
  where the hidden objects are, so there is nothing to search for.
- Hedgehog: a well-drawn spine is rejected when it does not start exactly on the small start dot;
  repetitive and not intuitive. "A child cannot pass this level."
- Path/mud cleaning (last entrance enclosure): the mud is flat brown squares, unlike the drawn sand
  and leaves; the revealed background has no footprints (art request, pending).
- The art does not fill the screen; the buttons look detached from the game; "repeat" is useless;
  "next" should be big or automatic.
- Stars are collected on some levels; the user prefers the detective loop (collect an animal's
  clues, then guess which animal left them) or collecting the animals along the path.

## Constraints

- Everything from `odd/tasks/adventure-flow-and-map-guidance.md` §Constraints still applies: no
  `url(#…)`/mask/clipPath/pattern/filter in SVG, node-env tests (`renderToString`, no DOM),
  generators emit only M/L/C, Rioplatense child copy, level ids are persisted keys (never delete one).
- No punishment, no red, no failure sound (`docs/01` principle 2).
- Out of scope for this stage: accounts/profiles, PWA/offline, cursive letters (`docs/15`).
- Another session works in `/home/opencode/projects/cursiva` on `exp/svg-art` (SVG art, including the
  map footprints). Never touch that working tree; each task runs in its own worktree under
  `/home/opencode/projects/cursiva-wt/`.

## Mode and checks

TDD off (`openspec/config.yaml`: `tdd: false`, `strict_tdd: false`); runner vitest. Per task:
`npm test`, `npm run build`, `git diff --check`, and browser QA with the system Chromium
(`/usr/bin/chromium`, `--disable-gpu`) on the touched levels at 1024x768 and 844x390 (screenshots
under `capturas/2026-09-25-tanda1/`, outside git). RDD is off (user, 2026-09-23).

## Delivery

One branch per task off `main`, merged into `main` by the orchestrator after its checks, then pushed
at the end of each batch. Strategy: `single-pr`-free direct merges to `main` (the user asked for
merge + push so the deploy updates and they can test).

## Batch 1: what blocks play (parallel, one writer each)

| ID | Task | Branch | Route | Status | Evidence |
|---|---|---|---|---|---|
| T1 | Snakes: align the corridor with the snake art so a child can pass `snake1..4` | `fix/snakes-corridor-alignment` | delegated (4+ files to map, writer trigger) | done | `4d28716` — `evaluateLevel` resampled all strokes as one arc, so a third of the samples fell on sand between snakes (a perfect trace scored 0-52); now per-stroke. `2bae9bb` — the dark channel paint under art corridors poked out of the snake outline (the "misalignment" the user saw); gated off for `artCorridor`. Start octopus no longer covers the head on snake1/2/4 (snake3 unchanged). Full suite and build green on main |
| T2 | Flashlight: the first tap must not reveal everything; no halo giving away hidden objects; a found object stays lit in a radius around it | `fix/night-flashlight` | delegated | done | `38919bc` — night1's only chest sat at the sheet centre inside the 200-unit find radius, so any first tap won (moved to (180,460)); the on-top `data-night-visible-hint` halo removed; found objects keep their own falloff in `revealTiles`. Full suite 2147 green, build green, QA screenshots in the branch worktree. Open: the lit area is tile-stepped, not round (batch 2) |
| T3 | Hedgehog: accept a spine that starts near (not exactly on) the start dot; make it less repetitive and clearer | `fix/hedgehog-spines` | delegated | done | commit `2ce1e92` on `fix/hedgehog-spines`; `npx vitest run --maxWorkers=2` on the 4 touched files: 342 passed; `npm test -- --maxWorkers=2`: 2154 passed (87 files); `npm run build`: clean; `git diff --check`: clean; browser QA (system Chromium, 1024x768, `?nivel=hedgehog1`/`?nivel=hedgehog4&dev`) in `capturas/2026-09-25-tanda1/`: an off-centre (~25 viewBox units) first stroke on hedgehog1 is accepted (`02-hedgehog1-01-after-offcentre-first-spine.png`), and both hedgehog1 (4 spines, was 5) and hedgehog4 (9 spines, was 14) complete to a green "Siguiente" (`03-hedgehog1-02-final.png`, `06-hedgehog4-02-final.png`) |
| T4 | Mud of the path enclosure (`sand3`) drawn in code like the sand and the leaves, not flat squares | `fix/cleaning-levels` | delegated | done | `1b4ef97` — new `visual: 'mud'` RevealLayer policy (pebbles, puddles, eroded silhouette); `npm test`/`npm run build` green; browser QA (`?nivel=sand3`, glass1/sand1/glass3 for regression) in `capturas/2026-09-25-tanda1/` |
| T6 | Cleaning levels (glass, sand, mud): the finger only cleans: no ink line, no drawing of any kind (user, 2026-09-25: "que solo limpie pasar el dedo, no que deje un trazo ni dibuje") | `fix/cleaning-levels` | delegated (same writer as T4) | done | `bf92bf9` — `resolveInkPolicy('erase')` now returns `'none'` (was `'live-only'`); verified `data-ink-policy="none"`, empty `d` mid-drag on glass1/sand1/glass3/sand3; flashlight (`'light'`) untouched |
| T5 | Dev-only "skip level" button in level play, visible in dev builds or with `?dev` (same gate as the progress reset) | `feat/dev-skip-level` | delegated | done | `client/src/screen/GameScreen.tsx`: button gated by the existing `isDevMode()` (same gate as `App.tsx`'s "Reiniciar progreso (dev)"), routed through the real `handleAttempt`/`handleNext` closures via a synthetic full-pass `SKIP_ATTEMPT` folded through `applyAttempt` — same completion path a real pass uses (progress saved, next level, closings, map, focus advance). `?dev` survives navigation for free: this SPA never mutates `window.location`, so `isDevMode()`'s live read of `window.location.search` stays valid across every screen without extra persistence. Intro/closing screens are single-tap (`.cv-intro-stage`, `.cv-closing-stage`). Tests: `client/src/screen/GameScreen.test.tsx` (present/absent by `isDevMode()` via `vi.spyOn`, absent on intro view, `SKIP_ATTEMPT` folds to one real approval through `applyAttempt`). Checks: `npx vitest run --maxWorkers=2 src/screen/GameScreen.test.tsx` → 65 passed; full suite `npx vitest run --maxWorkers=2` → 2147 passed (87 files); `npm run build` → OK; `git diff --check` → clean. Browser QA on a `vite preview` production build (port 5196), system Chromium headless `--disable-gpu`, empty localStorage, `?dev`: walked prologue skip → map → full `entrada` adventure (glass1→sand1→glass3→sand3) via the skip button at every level → back on the map, `?dev` still in the URL, at both 1024x768 and 844x390; confirmed the button is absent (count 0) without `?dev` on the same build. Screenshots in `capturas/2026-09-25-tanda1/` (git-ignored, not committed): `prod-01..05-*`. Commits: `3cfdea0` (code+tests), docs commit follows. |

## Batch 2: screen and UI (three writers, disjoint files)

Decisions taken by the orchestrator (user delegated, 2026-09-25; revisit after their test): auto-advance
after a short success celebration (a tap skips the wait); the "repeat" button goes; the demo (play) button
stays, restyled.

| ID | Task | Branch | Route | Status | Evidence |
|---|---|---|---|---|---|
| T7 | Level screen: the backdrop art fills 100% of the viewport (the playable sheet may shrink relative to the art; nothing clipped at 1024x768, 1180x820, 768x1024, 844x390); chrome floats over the art in the game's marker style (`docs/09`); drop "repeat"; auto-advance after the success celebration, tap to skip | `feat/fullscreen-level-ui` | delegated (writer trigger) | done | Three passes. **Pass 1** (`a9af067`) shipped a `.cv-backdrop-fill` CSS layer behind the unchanged sheet — rejected: drew the art TWICE (picture-in-picture). **Pass 2** (`364ceef`+`c9d79f4`, after merging `main`'s T8+T9 in `8a875ad`) replaced it with an expanded SVG `viewBox` inside `canvas/TraceCanvas.tsx` — one continuous picture — but the sheet still sat inside the OLD flex-column layout, so flat-colour header/footer bands and side margins remained, and the cleaning-level margin (a flat rect merged via `marginBands`, big rectangles) produced a self-intersecting oval blob against the real grid (`boundaryLoops`' edge-cancellation needs matching-size tiles, not a few large ones). **Pass 3** (`e193d26`+`c9d79f4`+`741b92d`) is what shipped: `LevelPlay.tsx`'s `.cv-sheet` is now `position:absolute;inset:0` — the WHOLE viewport, no padding — with `.cv-top`/`.cv-foot`/the portrait-rotate card as their own absolutely-positioned floating layers (z-index above the sheet, `pointer-events:none` on the row with `auto` restored per child) instead of flex-column siblings; `canvas/TraceCanvas.tsx`'s `fitContentWithInsets`/`fitCameraContentWithInsets` (replacing pass 2's `expandToAspect`/`expandHeightToAspect`) fit the playable content into the viewport MINUS the chrome's own measured safe insets (`LevelPlay` measures `.cv-top`/`.cv-foot`'s rendered heights via `ResizeObserver`, small fixed side insets) before expanding to the full container aspect, so the backdrop still covers every pixel including behind the chrome while nothing playable sits under a button; the floating result pill moved from `.cv-sheet` (now full-viewport) into `.cv-foot`, anchored to that row's own top edge (`bottom: 100%`) instead of the viewport bottom, so it no longer overlaps the actions row. `RevealLayer.tsx`'s cleaning margin now uses `marginGridTiles` (grid-aligned to the real tiles' own cell size and lines) instead of `marginBands`, merged as extra non-scoring tiles into the SAME organic generator the real grid uses — one continuous shape/fill/opacity, no seam, and it clears together with the real grid on completion (verified: a fully-cleaned glass1 shows zero dark margin left over). Also restyled `voice/SpeakButton.tsx`+`voice/VoiceToggle.tsx` and `screen/ZooMap.tsx`'s HUD pills (backpack/recovered/star) to the marker style (warm paper, thick dark outline, no shadow) — the map's dev-only reset button (`App.tsx`) stays untouched, debug chrome by design. Tests: `TraceCanvas.test.tsx` (fitContentWithInsets/fitCameraContentWithInsets: zero-insets reduces to the old centred-expand behaviour, inset-driven off-centre shift, camera never grows/shifts width, guards, one SSR byte-identity check), `RevealLayer.test.tsx` (marginGridTiles: grid alignment, sub-cell rounds up to one full cell, never emits inside sheetBounds, guards; displayBounds: organic merge for glass/sand/mud, "clears together", plain-fallback flat patch grid-tiled). Checks: full suite `npx vitest run --maxWorkers=2` → 2231 passed (88 files); `npm run build` → clean; `git diff --check` → clean. Browser QA (system Chromium, `--disable-gpu`): re-shot glass1/sand3/duck-trail1/snake1/night2/dolphin1/sheep-hill1 × all 4 required viewports (28 screenshots) plus the real illustrated map at 1024x768, in `capturas/2026-09-25-tanda4/` (git-ignored) — looked at every one: no flat bands or side margins on any level at any viewport (confirmed on duck-trail1/snake1, the two the orchestrator flagged directly), no seam or blob in the cleaning margin at any viewport including the extreme 768x1024 portrait, night2's darkness and glass1's grime both reach every edge including at the tight 844x390 tier, dolphin1 (camera) has no side gaps at any viewport, and the map's backpack/star/mute chrome now matches the level screen's marker style. Re-verified trace-to-completion and failed-attempt-retry against the final layout: still pass identically (glass1 auto-advances after a real approval with zero further input, ending fully clean with no leftover margin; the repeat button stays gone and a fresh touch still clears a failed attempt's ink). |
| T8 | Life and feedback outside the level screen: Pulpito breathing/blinking on prologue, intro and closing; speech-bubble pop-in; star-counter flash on the map when a star is won; short transitions between screens | `feat/character-life-animations` | delegated (must not touch `LevelPlay.tsx`/`TraceCanvas.tsx`) | done | `773dc0b` on `feat/character-life-animations` (resumed after a container restart killed the previous writer mid-suite; work was already ~complete uncommitted in the worktree, reviewed against this task line by line, then committed). Breathing (`cv-octopus-breathe`, restates the box's own static `translateX(-50%)`) and an occasional blink (`cv-octopus-blink`, on a nested transform-free `.cv-octopus-life` img) on `PrologueOpening.tsx`, `AdventureIntro.tsx`, `AdventureClosing.tsx` — each animates its OWN element, never the same `transform` property twice (the SVG-transform trap class this task's brief calls out). Bubble pop-in (`screen/BubblePop.ts`'s shared `BUBBLE_POP_CSS`, scale 0.7→1.08→1, ~200ms) on those three screens plus `ZooMap.tsx`'s own Pulpito bubble, keyed on the spoken line/label so a text change replays it without remounting the narrating component. Star pill pop+spark on `ZooMap.tsx` only on a genuine rise (`zoo/stars.ts`'s `starsIncreased`/`seenStars`/`recordSeenStars`, module-scope session memory since `ZooMap` remounts every map visit). Screen transitions: `App.tsx`'s `shellTransitionKey` and `GameScreen.tsx`'s own `screenTransitionKey` key a shared `screen/ScreenTransition.tsx` wrapper (opacity-only 200ms fade) by SCREEN IDENTITY, not by render — stable across an in-place update (a prologue plate, a closing beat) so `useNarration`'s mount-time speak never double-fires or gets cut; verified directly by dedicated tests on both key functions (`GameScreen.test.tsx`'s "beat change keeps the same key" case is the load-bearing one). All CSS carries a `prefers-reduced-motion: reduce` override that disables the animation outright. `LevelPlay.tsx`/`TraceCanvas.tsx` untouched (confirmed via `git diff --stat` before committing); `GameScreen.tsx` only gained a `ScreenTransition` wrapper per branch, no logic changes. Checks: targeted `npx vitest run --maxWorkers=2` on the 8 touched test files → 185 passed; full suite `npx vitest run --maxWorkers=2` → 2196 passed (88 files); `npm run build` → clean (`tsc --noEmit && vite build`); `git diff --check` → clean. Browser QA: system Chromium (`/usr/bin/chromium`, `--disable-gpu`) at 1024x768 via Playwright against `vite --host 127.0.0.1 --port 5198 --strictPort`; deep links `?nivel=apertura`, `?nivel=intro-glass1`, `?nivel=cierre-glass1`, and the real `ZooMap` (reached via `?debug=progreso:glass1` then a bare reload, since `?nivel=mapa` is the OTHER, internal level-list map); 3-4 frame sequences per screen in `capturas/2026-09-25-tanda2/` (git-ignored) — frame checksums differ within each sequence (motion confirmed, one frame caught the blink mid-squash on the prologue octopus) and are byte-identical across a `reducedMotion: 'reduce'` context (animation correctly suppressed) and across an ordinary ZooMap load with no star increase (no false-positive flash, matching `ZooMap.test.tsx`'s own assertion); zero console/page errors. Not independently re-triggered in-browser: the star flash's true-positive path (earning a star mid-session then returning to the map) — the pure `starsIncreased` decision is unit-tested directly instead, the same single-URL-capture convention this codebase already documents for screens a playthrough cannot reach in one deep link. |
| T9 | Flashlight: the permanently lit area around a found object is round and soft, not tile-stepped | `fix/flashlight-round-light` | delegated (`RevealLayer.tsx`/`revealGrid.ts` only) | done | `d456a6b` — `revealTiles`'s `light` branch now returns one bounding square per active source (found object or live torch) instead of tiling `cols x rows`; `RevealLayer.tsx` reconstructs the circle and punches a round hole plus three falloff rings into one full-sheet dark path (`fillRule="evenodd"`, no mask/clipPath). Node count for the veil: up to 135-240 `<rect>`s (old grid) → flat 4 `<path>`s regardless of source count. Also fixed the completion frame (`LevelPlay.tsx`'s `allRevealTiles` bypass) to render no veil at all instead of a grid of meaningless tiny holes; updated the one `LevelPlay.test.tsx` assertion that encoded the old grid-tile-count contract. `npx vitest run --maxWorkers=2` on the 3 touched test files: 185 passed; full suite `npx vitest run --maxWorkers=2`: 2179 passed (87 files); `npm run build`: clean; `git diff --check`: clean. Browser QA (system Chromium, `--disable-gpu`, 1024x768 and 844x390, `?nivel=night2`): solid darkness before anything found, round soft light while the torch is held, the found object stays round-lit after lifting, live torch + a found object compose correctly side by side, and the completion frame (both objects found) shows the full backdrop with zero dark veil. Screenshots in `capturas/2026-09-25-tanda2/` (git-ignored) |

## Batch 2.5: re-opened defects from batch 1

| ID | Task | Branch | Route | Status | Evidence |
|---|---|---|---|---|---|
| T11 | Snakes: the traced route (guide/scoring path) truly follows the drawn body's centreline on `snake1..4`, including `snake3`'s rotated column | `fix/snakes-true-alignment` | delegated (4+ files to map, writer trigger) | done | **Root cause found by measuring, not re-guessing**: batch 1's T1 left the corridor SCORING correct but the route's SHAPE still came from a closed-form fit (`build_art.py`'s old `sample_spine`) that reduced the measured per-column centreline to five numbers (`mid` + alternating half-arch `[width, rise]` pairs) and replayed them through `spineWave`. That fit's own halves summed to the span between its FIRST and LAST real zero-crossing, which — by the fit code's own documented design — sat INSIDE `[traceFrom, traceTo]` by a margin (measured: 34.8/52.8 shipped px on the small snake, 4.3/52.6 on medium, 0.8/11.8 on large, out of ~344-374px traceable spans). But `placeArtCorridor`/`snakePathD` always started replaying the halves at `traceFrom`, never at that inner crossing — so the replayed curve silently drew a SHIFTED stretch of the fitted wave. The fit's own reported residual (1.7-3.4px) never caught this because it only measured the fit against the data it was fitted FROM, not against where the runtime anchored the replay. Measured independently (a standalone column-scan script against the shipped PNGs, not the code under test): max deviation between the OLD rendered guide and the true measured centreline was **29.4px (small), 6.8px (medium), 2.3px (large)**, shipped-px ≈ viewBox units ≈ screen px at 1024×768 (SVG scale ≈ 0.99) — this is what the user saw as "larger amplitude, different phase, dipping to the lower edge, rising to the upper edge" (a whole-route RMS hides exactly this: the ends and crossings still matched). Two of three snake bodies also have essentially ZERO measurement noise (max 1.5px step between adjacent raw columns) and no self-occlusion (checked directly: only the large snake has any multi-run columns, and those sit in the head taper, outside the traceable span) — so the per-column vertical-midpoint measurement itself was never the problem, only its reduction to a formula and the anchor mismatch on replay. **Fix**: `scripts/art/build_art.py`'s `sample_spine` no longer fits a formula — it samples the measured (lightly 3-tap-smoothed) centreline directly at ~one point per 8px across the FULL `[traceFrom, traceTo]` span and stores it as `points: [[xFrac, yFrac], ...]`, first/last point anchored exactly at `traceFrom`/`traceTo` so the anchor-mismatch bug is structurally impossible (nothing left to re-derive). `client/src/levels/artCorridor.ts`'s `DrawnSpine`/`DRAWN_SPINE` and `client/src/levels/catalog.ts`'s `snakePathD` were rewritten around `points` instead of `halves`; `client/src/levels/paths.ts` gained `spinePolyline` (M/L only) alongside the existing `spineWave` (kept, still used/tested elsewhere). `client/public/art/manifest.json`'s three snake entries were regenerated by calling the fixed `sample_spine` directly against the shipped PNGs (not a full pipeline re-run, which would have touched unrelated assets). New residual after the fix: **0.75px (small), 0.95px (medium), 0.91px (large)** — all under 1px, the noise floor of the smoothing step itself. Growing each piece's true path length (now reaching all the way to `traceTo`) shifted the sheet's own centring: `layOutPaths`'s `tx` moved from -9.62 to +9.62 for the horizontal family (R6), so `snakeHorizontalPieces()`'s shared `at.x` was re-measured from 455.66 to 446.04 (found by measuring the actual `buildLevelTarget` output, not guessed, same method the existing `at.x`/`at.y` comments already document); the vertical family's `tx` was already under 0.01 and needed no change. Also generalized the start-octopus placement: `LevelPlay.tsx`'s `standBesideArtCorridor` call was gated `piece0.rotate` off for `snake3` by a previous writer pending verification (`placeArt.test.ts` already proved the rotation math correct in isolation — "rotate:-90 column shifts on Y, not X"); removed the gate and verified in the browser that the octopus now stands beside `snake3`'s head (bottom of the column, not top — the catalog.ts comment claiming "head at top" for the vertical family turned out to be a stale assumption; the actual rendered head sits at the BOTTOM of each rotated column, tail/star at the top, confirmed directly from screenshots) with no overlap. Tests: `artCorridor.test.ts` rewritten around `points`/`spinePolyline` (box aspect, mid-anchor, tx-shift, rotation isometry, residual/thickness scaling, the two-derivation "coincidence" proof and its shifted-centreline sensitivity check) plus a NEW fixture describe block — 5 hand-measured `(xFrac, yFrac)` probes per snake, independently column-scanned off the shipped PNGs by a standalone script (not the pipeline code under test), asserting `DRAWN_SPINE.points` stays within 3px of each (measured agreement < 0.7px on all 15 probes). `catalog.test.ts`'s C2 (wave-crest clearance) was rewritten from the old closed-form `waveCrestRadius` to a Menger-curvature circumradius over consecutive `points` triples — the same "how tight is the tightest bend" property, answered from the measured centreline instead of a formula. `artManifest.test.ts`'s snake-specific check now validates `points` (strictly ascending in x, first/last anchored to `traceFrom`/`traceTo`) instead of `halves`. Checks: `npx vitest run --maxWorkers=2` on the 4 touched test files → 268 passed; `npx vitest run --maxWorkers=2 src/levels/catalog.test.ts` → 147 passed; full suite `npx vitest run --maxWorkers=2` → 2234 passed (88 files, same 4 pre-existing unrelated Playwright-spec "failures" vitest always reports in this repo — not real test failures, not touched by this change); `npm run build` → clean; `git diff --check` → clean. Browser QA (system Chromium, `--disable-gpu`, 1024×768, `?nivel=snake1..4&dev`, dev server on port 5198): dragged each level's 3 scattered pieces into place via simulated pointer drags (largest-first, since `arrange.ts`'s `grabPiece` hit-tests unplaced boxes in reverse/largest-first order and the scattered boxes overlap) to reach the real placed/rotated render, not just the scatter screenshot. `snake1`'s demo animation (the actual white route line the user's screenshot showed, `TraceCanvas.tsx`'s `demo` stroke in `SHEET_PAPER`) now visibly hugs the drawn body's centreline along its whole length on all three pieces, crest to trough — a stark contrast with the reference "before" screenshot (`cursiva-wt/fix-snakes-corridor-alignment/capturas/2026-09-25-tanda1/after-zoom-snake1.png`) where the line's amplitude visibly overshot the body and touched its edges. `snake2`/`snake4`/`snake3` don't render a persistent visible guide during normal play (`LevelPlay.tsx:2725` explicitly suppresses `guide` whenever `level.artCorridor` is set — a deliberate, pre-existing design choice, not something this task changes), so their fix is verified through the shared code path plus the data-level tests above, which the whole family reuses identically. `snake3`'s start octopus now stands clearly beside the first (small) column's head with visible sand between them, at all three levels' full-screen shots. Screenshots (git-ignored) in `capturas/2026-09-26-tanda3/`: `snake{1,2,3,4}-1024x768-full.png` plus a per-piece `-pieceN-zoom.png` crop for each. **Observations for the snake story (batch 3), not acted on**: (1) ~~on `snake4` only, a fourth medium-snake image renders near the large snake's tail~~ — investigated per orchestrator follow-up and EXPLAINED, not a bug: `snake4` is the last level of the `snake` adventure (`zoo/adventures.ts`: `animal: 'vibora'`), and `detective/assets.ts:435` defines `ZOO_ANIMAL_ART.vibora = SECTOR_ADVENTURE_ART.snakeMedium` — `LevelPlay.tsx`'s `endArt`/`isLastOfAnimalAdventure` logic shows the recovered animal's own art (the SAME medium-snake PNG, reused as the reward icon) at the route's end, sized `END_MARK_SIZE = 84` (matches the measured 84px height exactly) — only true on the adventure's LAST level, never `snake2`. Confirmed untouched by this branch: `git diff 68ee9a8 HEAD -- client/src/zoo/adventures.ts client/src/detective/assets.ts` is empty. No fix needed. (2) The vertical family's actual head-at-bottom/tail-at-top orientation (opposite of `snakeVerticalPieces`'s own doc comment) is now noted in that comment's neighbourhood in `LevelPlay.tsx`; the catalog.ts comment itself was left as-is (out of scope) but is worth a follow-up correction. |
## Batch 2.5: flashlight polish (one writer, `RevealLayer.tsx`/`revealGrid.ts`/`LevelPlay.tsx` reveal wiring only)
| T10 | Flashlight polish, from the user's tablet play-test: (1) the torch overlapping a found object's own light rendered the overlap BLACK; (2) no animation — a found object popped in at full size, completion popped straight to daylight; (3) a decorative circle sat in the centre of the torch even where nothing was there; (4) softer edges without SVG filters | `fix/flashlight-polish` | delegated (writer trigger) | done | `ef7411c`+`eb29bea`. **Root cause of item 1**: T9's veil punched one `evenodd` hole per source; two overlapping holes crossed an EVEN number of boundaries and rendered back to filled/dark — a fill-rule parity bug, not fixable by flipping to `nonzero` either (overlapping same-direction holes still drive the winding number away from zero instead of clamping at "at least one hole"). Fixed by computing a per-point scalar (`veilDarknessAt`, exported: the MIN normalized distance ratio across every source, quantized via the existing `lightOpacity`) over a coarse 25-unit grid, then tracing each of the (up to 4) darkness LEVELS' own tile set with the SAME `boundaryLoops` union already used for the fog/sand/leaf/mud policies — a proper set union, immune to the overlap bug by construction, not by fill-rule bookkeeping. Node count stays flat at ≤4 `<path>`s regardless of source count (measured: 0-3 rings + 1 base in `renderToString`, same ceiling as before). Edges read soft via Chaikin corner-cutting (`smoothLoop`, 3 passes, no jitter) + quadratic joints — item 4, no `<filter>`/`<mask>`. **Item 3**: removed the `data-night-torch` decorative glow group outright — it painted a warm highlight at the torch's own position UNDER the veil regardless of whether anything was there, redundant once the darkness bands (T9, now T10's union) already carry the falloff; it predates T9 and was never revisited. **Item 2**: `levels/revealGrid.ts`'s `RevealState` gained `litAt`/`completeAt` timestamps (stamped by `revealTick`'s new required `now` param) and an eased `growthFraction`; a found object's own light grows from 0 to `reveal.radius` over `LIGHT_FOUND_GROWTH_MS` (400ms), and once every object is found, `canvas/RevealLayer.tsx` (the one place that knows `displayBounds`) grows each found source's radius further out to the farthest screen corner over `LIGHT_COMPLETE_GROWTH_MS` (1.4s) before `screen/LevelPlay.tsx` falls back to the pre-existing `allRevealTiles` "no veil at all" bypass — no more popping straight from dark to full daylight. `prefers-reduced-motion` snaps both grows to their end state. **A second, more subtle bug found only via live browser diagnosis** (the pure unit tests all passed against same-scale fixtures and did not catch it): `screen/LevelPlay.tsx`'s `onRelease` handler calls `releasedRevealState` on every stroke release (a raw-point rescore that replaces `revealState` wholesale), and it stamped `litAt`/`completeAt` with `Date.now()` while `onFrame`'s own `timeMs` — and therefore every other `revealTick` call site, and the `animNow` clock the growth math is eased against — is `performance.now()`-based (`canvas/TraceCanvas.tsx`'s rAF loop, page-relative, not the Unix epoch). The ~1.7e12 scale mismatch made `elapsed = animNow - litAt` a huge negative number forever, which `growthFraction` clamps to 0: a found object's glow never grew past radius 0 in the real app. Found by logging live `litAt`/`completeAt`/`timeMs` values from the running page (`console.log` inside `onFrame`, read back via Playwright) after a browser QA pass showed the veil's rendered darkness never changing shape over nearly 2 seconds. Fixed by switching `releasedRevealState` and `animNow`'s own initial value to `performance.now()`. Animation timing/growth math itself: `revealGrid.test.ts` (`growthFraction` monotonic/clamped/reduced-motion; `litAt`/`completeAt` latch timing; `lightSources`/`revealTiles` grow-in; `isLightAnimating`/`completionGrowthFraction`). Union fix: `RevealLayer.test.tsx` (`veilDarknessAt` exported and tested directly — two overlapping sources give darkness 0 at a point well inside both radii, matching neither individually-computed darkness being exceeded; a point outside every source's reach stays fully dark; idle/zero-source fast path unchanged; node count bounded; the `allRevealTiles` completion bypass still renders no veil at all). Checks: targeted `npx vitest run --maxWorkers=2` on the 5 touched test files → 530 passed; full suite `npx vitest run --maxWorkers=2` → 2247 passed (88 files); `npm run build` → clean; `git diff --check` → clean. Browser QA (system Chromium, `--disable-gpu`, 1024x768, `?nivel=night1`/`?nivel=night2`, Playwright driving real `mouse` events mapped through the SVG's own `viewBox`): idle start is plain dark with no leftover centre circle; the live torch held directly over an already-found object shows one smooth continuous lit region with no dark pocket at the overlap (the exact reported scenario); a completion frame mid-grow shows the real night backdrop (moon, lantern, waterfall) through a large soft-edged circle with the far corners still genuinely dark, not an instant jump to full daylight; confirmed via a live diagnostic script that the darkest band's own rendered extent shrinks monotonically over ~1.3s after the clock fix (it did not move at all before the fix). Screenshots in `capturas/2026-09-26-tanda3/` (git-ignored). Known minor residual: a level can auto-advance to the next one before the completion grow's later frames are visually inspectable in a scripted QA pass (T7's own auto-advance) — not a defect this task introduced, just a QA-script timing note. |

## Batch 2.6: night replay/glow and hedgehog polish (parallel, three writers, disjoint files)

| ID | Task | Branch | Route | Status | Evidence |
|---|---|---|---|---|---|
| T12 | Night: lifting the finger after finding something must not darken and re-grow the light again; found things must not carry a semi-transparent yellow disc | `fix/night-and-hedgehog-polish` | delegated (`canvas/RevealLayer.tsx`/`levels/revealGrid.ts` only) | done | **Root cause of the replay** (`screen/LevelPlay.tsx`'s `releasedRevealState`, one caller: `onRelease`): every stroke release re-folds the WHOLE raw stroke snapshot from `EMPTY_REVEAL` at one shared `performance.now()` clock (by design — see T10's own header, RAW points not the rail-warped live copy) and returned that fresh fold outright. `revealTick`'s own stamping rule ("stamp `litAt`/`completeAt` only when newly reached") then fired on THIS release's `now` for every object the fresh fold still found lit, and for `completeAt` whenever the fresh fold still read "every object found" — even a release where nothing new was found, anywhere on the sheet, arbitrarily long after the real grow-in had already finished. That replayed a found object's own 400ms grow-in and the scene-wide 1.4s completion wash on every single release, exactly the reported "goes dark for a second and grows again". Fixed by giving `releasedRevealState` a fourth `prev: RevealState = EMPTY_REVEAL` parameter (the live incremental fold, `revealStateRef.current` at the real call site) and merging: an index the fresh fold still reads as lit keeps `prev`'s OWN `litAt` timestamp when `prev` already had one (only a genuinely NEW index keeps the fresh stamp), and `completeAt` keeps `prev.completeAt` once it is non-null (only the FIRST completing release keeps the fresh stamp). Defaulted rather than required, so the one pre-existing test caller stays byte-identical. **The yellow disc**: `canvas/RevealLayer.tsx`'s `revealedArt.map(...)` (found-object render) drew two extra circles per found object under `nightVeil` — `NIGHT_FOUND_GLOW` (`#fce97a`) then `#fffbe6` — on top of the veil's own hole. Removed outright (constant deleted too): the veil's own darkness bands already ARE the light that found it; no replacement cue added, per the brief's own "the light itself is what shows it's found". Tests: `screen/LevelPlay.test.tsx` — two new regression tests (`releasedRevealState` with a mocked `performance.now()`: a later release that finds nothing new preserves every `litAt`/`completeAt` instead of restamping; a later release that finds one MORE object keeps the earlier one's original timestamp while stamping the new one fresh, and stamps `completeAt` fresh only on the release that first completes). `canvas/RevealLayer.test.tsx` — asserts `#fce97a`/`#fffbe6` are gone and the discovery `<g>` now wraps only an `<image>`, no `<circle>`. Checks: targeted `npx vitest run --maxWorkers=2` on the touched files → 160+142 passed; full suite `npx vitest run --maxWorkers=2` → 2267 passed (88 files); `npm run build` → clean; `git diff --check` → clean. Browser QA (system Chromium, `--disable-gpu`, 1024x768, `?nivel=night2`, Playwright mouse events through the SVG's own live `viewBox`): found the first object, captured frames at 0/200/600ms after lifting — byte-visually identical, no darkening or regrow; a second lift far from either object shows the same stable frame again; found the second (last) object and captured the completion wash growing outward with no yellow disc on either found object at any point. Screenshots in `capturas/2026-09-26-tanda26/night2-*.png` (git-ignored). |
| T13 | Hedgehog: a stroke that isn't accepted as a spine should fade away quickly instead of littering the sheet; the demo animation is too slow | `fix/night-and-hedgehog-polish` | delegated (`canvas/SpineLayer.tsx`/`levels/spines.ts`/minimal `screen/LevelPlay.tsx` spine wiring only) | done | **Reject-fade**: `levels/spines.ts` gained `acceptedSpineStrokeIndices(strokes, cfg)` — the exact same greedy walk `spineSettle`/`spineScore` already run (same `nearestUnfilledAnchor`/`passesRemainingMeasures` calls, so what counts as a spine can never drift between the two), restated to report which STROKE indices earned an anchor rather than only the anchor set. `screen/LevelPlay.tsx`'s `shownStrokes` now filters permanent settled ink through it for a `level.spines` level (render-only — the `strokes` state itself, and what `evaluateLevel` scores, is UNCHANGED, since a rejected stroke already contributes nothing to `spineScore` either way). The just-released stroke itself is detected via the existing `grew` check in `onRelease`'s spine block (`next !== spineRef.current`, restated from an inline boolean to reuse); on a miss it is pushed into a small `fadingSpineStrokes` list (own stable id per entry, several can fade at once) and dropped again after `SPINE_REJECT_FADE_MS` (300ms) via `window.setTimeout`, cleaned up on every reset site (`resetSurface`/`restartRun`) and on unmount. `canvas/TraceCanvas.tsx`'s `TraceSpines` type gained two optional fields (`fading`/`fadingColor`, additive, absent = byte-identical) so `canvas/SpineLayer.tsx` can render each fading entry as its own centreline `<path>` (the SAME `inkPath(traceInk(...))` pipeline `TraceCanvas.tsx`'s own settled ink uses, imported from `canvas/ink.ts` — never a second re-derivation) tagged `.cv-spine-fading`, a one-shot `@keyframes` opacity fade in `LAYOUT_CSS` (not a `transition`: a keyframe animation runs from its own `0%` the instant the element exists, no double-render flip-flag needed) with a `prefers-reduced-motion` snap straight to invisible. **Demo speed**: `screen/LevelPlay.tsx` gained `SPINE_DEMO_DURATION_S`/`SPINE_DEMO_STEP_S` (0.8s/0.85s, roughly half `DEMO_DURATION_S`/`DEMO_STEP_S` at 1.6s/1.7s), selected by `level.spines` alone so every OTHER routed level's demo pace is untouched. Tests: `levels/spines.test.ts` (`acceptedSpineStrokeIndices`: reports the right indices in order, excludes a rejected stroke without disturbing its neighbours, its count always agrees with `spineSettle`'s own `filled.size`, empty input handled). `canvas/SpineLayer.test.tsx` (parses `<path data-spine-fading>` out of the HTML string the same way the file's existing coincidence-proof tests do: correct centreline `d`, `fadingColor` vs `dim` fallback, one path per entry in order, absent when the field is absent, no forbidden fragment references). `screen/LevelPlay.test.tsx` (hedgehog1's own demo array: first duration ≤1s and ≈0.8s, step ≈0.85s; an ordinary routed level's demo stays ≈1.6s/1.7s). Checks: targeted `npx vitest run --maxWorkers=2` on the touched files → 160+142 passed (same combined run as T12, disjoint files); full suite → 2267 passed (88 files); `npm run build` → clean; `git diff --check` → clean. Browser QA (system Chromium, `--disable-gpu`, 1024x768, `?nivel=hedgehog1`): screenshotted the demo every 250ms — all 3 spines fully drawn by ~1000ms after load (old pace would have shown only the first spine's OWN draw still in progress at that point); drew a stroke nowhere near the body — visible at release, gone by 650ms, and (checked directly via the DOM) the anchor marks stayed at `#989896`/unfilled the whole time; drew a real spine along an anchor's own ray — the mark turned `class="cv-spine-mark-filled" fill="#f2efe6"` and the ink stayed on screen past 800ms (not faded), confirming accepted vs. rejected strokes are told apart correctly in the running app, not only in unit tests. Screenshots in `capturas/2026-09-26-tanda26/hedgehog1-*.png` (git-ignored). |

## Later batches (planned, not started)

- Batch 3, mechanics: collect along the path (items persist after leaving the line, the last one ends
  the level; sheep on the peaks); detective loop per animal (levels 1-3 collect clues with no
  silhouette in the bar, then choose among three animals reusing `screen/Deduction.tsx`, level 4
  collects the animals; snake story goes here). Remove the animal silhouette from the path bar.
- Batch 4, art requests for the user (ChatGPT): monkey (A6), path background with footprints,
  weak clue icons (user to point out where the "drops" and "seeds" are).
- Then: play-test bee, dolphins, fish, turtles, monkeys and the finale.

## Progress

- 2026-09-25: `main` fast-forwarded to `a1b1eed` and pushed. Batch 1 started.
- 2026-09-25: batch 1 done (T1-T6) and pushed. Machine note: five parallel writers exhausted RAM+swap on the Pi (22 other Claude sessions hold ~3.9 GB); T3/T5 were stopped and resumed from their worktrees. Run at most 3 writers at once and vitest with `--maxWorkers=2`.
- Open from batch 1: the white guide line on the first snake is not centred on the body (user; to be redone with the snake mechanic in batch 3); the flashlight's lit area is tile-stepped, not round; snake3's start octopus; the mud puddle tint reads grey; night levels have no hint at all while dark (check with a child).
- 2026-09-25: T8 (batch 2) done in `feat/character-life-animations`. Resumed after a container restart killed the previous writer mid full-suite run; its uncommitted work was already essentially complete, reviewed against this task and committed as `773dc0b`. See the T8 evidence cell above.
- 2026-09-25: T7 first pass (`a9af067`) rejected by orchestrator review — the `.cv-backdrop-fill` approach drew the art twice (picture-in-picture, mismatched seams). Second pass (`364ceef`+`5abab0e`, after merging `main`'s T8+T9 in `8a875ad`) fixed that with an expanded-viewBox approach inside `TraceCanvas.tsx`, but a second review found the art still did not fill the screen (flat header/footer bands remained) and the cleaning margin showed a seam. Third pass (`e193d26`, `c9d79f4`, `741b92d`) made `.cv-sheet` truly fill the viewport with all chrome floating above it, fit content into the viewport minus the chrome's own measured insets, grid-aligned the cleaning margin into the real silhouette (fixing a self-intersecting-blob bug found along the way), and restyled the map's own HUD pills/voice toggle to match. Re-verified against 7 named families at all 4 required viewports plus the map; no flat bands, no seams. T7 done. Not merged to `main` yet.

- 2026-09-26: batch 2 done (T7-T9) and pushed. T7 took two review rounds: the first drew the art twice (a cover copy behind the unchanged sheet), the second still left flat header/footer bands; the final version makes the sheet `inset:0` and fits the content inside the chrome's measured insets. Open from batch 2: the map keeps flat green bands (fixed 5:3 stage); the path bar reads faint over busy art at 844x390; the dev reset button is still a plain pill (dev-only).
- 2026-09-26: T11 (batch 2.5, `fix/snakes-true-alignment`) done — closes the batch 1 "white guide line not centred" reopened item. The route data itself was reduced to a closed-form fit whose replay was anchored at the wrong x; replaced with a directly-stored measured polyline (see the T11 evidence cell). Also generalized the start-octopus placement to `snake3`'s rotated column, fixing "snake3's start octopus" from the same open list. Not merged to `main` yet.
- 2026-09-26: T10 (batch 2.5, flashlight polish) done in `fix/flashlight-polish`, not yet merged. Fixed the overlap-renders-black bug (T9's evenodd hole-punch could not union overlapping sources; replaced with a grid-classified union reusing `boundaryLoops`), removed the leftover decorative torch glow, and added the found-object/completion grow animations — plus a clock-scale bug (`Date.now()` vs `performance.now()`) found only by live browser diagnosis that had silently kept the growth stuck at radius 0. See the T10 evidence cell above for full detail, checks, and screenshot paths.
- 2026-09-26: T10 pass 2 (orchestrator review of the first pass's own screenshots). Three more defects found by actually looking at the frames: the grid-classified union fixed the overlap but left the edge visibly stair-stepped; a 7x2 grid of star dots and a pale hard-edged wash appeared the instant every object was found, well before the completion glow had actually finished growing over the scene. Fixed by replacing the grid classifier with an EXACT circle union (`polygon-clipping`, MIT, pinned `0.15.7`, added as a dependency) — each source's circle polygonized at 128 segments and unioned/differenced per band (6 bands now, 1 base + 5 rings, gently decreasing opacity) — and by gating the completion-only stars/wash on the grow having actually finished (`reveal.light.growth >= 1`), not just `reveal.light.complete`. `git commit 18aa904`. New tests: exact point-in-polygon check against the real rendered geometry (overlap has no dark pocket), a ±1-unit circle-accuracy check against the base band's own hole boundary, and three tests pinning the star/wash gate at each growth state. Full suite 2251 passed, build clean, `git diff --check` clean. Per-frame cost measured directly on this Pi (Node benchmark, `polygon-clipping`'s own union+difference over 6 bands): ~5ms for 1 source, ~12ms for 2, ~22ms for 4 — comfortably under the 60ms animation-tick throttle. Re-shot all required frames (idle, one found and lifted, torch overlapping it, completion at t=0/300/700/1100ms) in `capturas/2026-09-26-tanda3/` (git-ignored) and looked at each: genuinely round soft-edged circles with visible concentric falloff rings, no grid dots anywhere, no pale disk, growing smoothly toward full coverage.

- 2026-09-26: T12/T13 (batch 2.6, `fix/night-and-hedgehog-polish`) done, not yet merged. From a second tablet play-test on top of batch 2.5: night's `releasedRevealState` was restamping every already-found object's grow-in (and the completion wash) on EVERY stroke release, not just the one that found something — fixed by threading the live fold's own timestamps through instead of always re-stamping from a fresh `EMPTY_REVEAL` fold; the found-object yellow disc glow (`NIGHT_FOUND_GLOW`) was removed outright. Hedgehog: a rejected (non-spine) stroke now fades out over 300ms instead of staying on the sheet forever, and the demo plays roughly 2x faster (spine-specific pace, every other level's demo untouched). See the T12/T13 evidence cells above for full root-cause detail, checks, and screenshot paths.

## Next step

The user tests batches 1 and 2 on the deploy; then batch 3 (collect along the path + detective loop).